<Window x:Class="LichDeBan.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cài đặt" Height="250" Width="350" WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Cài đặt Lịch" FontSize="18" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Màu nền (Hex):" VerticalAlignment="Center" Width="100"/>
            <TextBox x:Name="ColorTextBox" Width="150" Padding="3" Text="#33FFFFFF"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Độ trong suốt:" VerticalAlignment="Center" Width="100"/>
            <Slider x:Name="OpacitySlider" Width="150" Minimum="0.1" Maximum="1.0" Value="1.0" TickFrequency="0.1" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Value, ElementName=OpacitySlider, StringFormat={}{0:N1}}" VerticalAlignment="Center" Margin="10,0,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,15">
            <CheckBox x:Name="StartupCheckBox" Content="Khởi động cùng Windows" VerticalAlignment="Center" />
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Hủy" Click="Cancel_Click" Width="70" Margin="0,0,10,0"/>
            <Button Content="Lưu" Click="Save_Click" Width="70" Background="#28a745" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
